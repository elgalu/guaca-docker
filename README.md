# guaca-docker
Docker with guacamole server

WIP...

You can open a browser into http://localhost:8484/#/login/ and login to guacamole with user "docker" and the same password as ${VNC_PASSWORD} so you no longer need a VNC client to debug the docker instance.
